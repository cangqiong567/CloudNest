# CloudNest 项目使用指南

## 📋 前置要求

在开始之前，请确保你的电脑上已安装以下软件：

### 必需软件

1. **Python 3.8+**
   - 下载地址：https://www.python.org/downloads/
   - 安装时勾选 "Add Python to PATH"

2. **Node.js 16+**
   - 下载地址：https://nodejs.org/
   - 建议下载 LTS（长期支持）版本

3. **Git**（可选，用于版本控制）
   - 下载地址：https://git-scm.com/

### 验证安装

打开终端（Windows 用户使用 PowerShell 或 CMD），运行以下命令：

```bash
python --version    # 应该显示 Python 3.8.x 或更高版本
node --version      # 应该显示 v16.x.x 或更高版本
npm --version       # 应该显示 8.x.x 或更高版本
```

---

## 🚀 第一次运行项目

### 步骤 1：打开项目目录

```bash
cd D:\python_work\创意\a_web
```

### 步骤 2：设置后端（Flask 服务器）

#### 2.1 进入后端目录
```bash
cd backend
```

#### 2.2 创建 Python 虚拟环境（强烈推荐）
```bash
# Windows
python -m venv .venv

# 激活虚拟环境
# Windows CMD:
.venv\Scripts\activate.bat

# Windows PowerShell:
.venv\Scripts\Activate.ps1

# macOS/Linux:
python3 -m venv .venv
source .venv/bin/activate
```

激活后，你的终端提示符前面会出现 `(.venv)` 字样。

#### 2.3 安装 Python 依赖
```bash
pip install -r requirements.txt
```

#### 2.4 配置环境变量
```bash
# Windows CMD:
copy .env.example .env

# Windows PowerShell:
Copy-Item .env.example .env

# macOS/Linux:
cp .env.example .env
```

然后用文本编辑器打开 `.env` 文件，修改以下配置（可选，使用默认值也可以运行）：

```env
# 应用密钥（用于加密）
SECRET_KEY=your-secret-key-here

# JWT 认证密钥
JWT_SECRET_KEY=your-jwt-secret-key-here

# 数据库连接地址（默认使用 SQLite）
DATABASE_URL=sqlite:///cloudnest.db
```

#### 2.5 初始化数据库
```bash
# 首次运行需要初始化数据库迁移
flask db init
flask db migrate -m "初始化数据库"
flask db upgrade
```

#### 2.6 启动后端服务器
```bash
python app.py
```

你应该看到类似以下的输出：
```
 * Serving Flask app 'app'
 * Debug mode: on
 * Running on http://127.0.0.1:5000
Press CTRL+C to quit
```

**✅ 后端服务器已启动！**

> ⚠️ **注意**：不要关闭这个终端窗口！保持后端服务器运行。

---

### 步骤 3：设置前端（Vue 开发服务器）

#### 3.1 打开一个新的终端窗口

> 💡 **重要**：你需要打开一个新的终端窗口，保持后端服务器继续运行。

#### 3.2 进入前端目录
```bash
cd D:\python_work\创意\a_web\frontend
```

#### 3.3 安装 Node.js 依赖
```bash
npm install
```

#### 3.4 启动前端开发服务器
```bash
npm run dev
```

你应该看到类似以下的输出：
```
  VITE v8.0.14  ready in 197 ms

  ➜  Local:   http://localhost:5173/
```

**✅ 前端服务器已启动！**

---

### 步骤 4：访问应用

打开你的浏览器（推荐 Chrome 或 Edge），访问：

🌐 **http://localhost:5173**

你应该能看到 CloudNest 的登录/注册页面。

---

## 🎯 日常使用（已配置好环境后）

每次想要运行项目时，只需要：

### 1. 启动后端

打开终端 1：
```bash
cd D:\python_work\创意\a_web\backend

# 如果使用了虚拟环境，先激活
# Windows CMD:
.venv\Scripts\activate.bat

# Windows PowerShell:
.venv\Scripts\Activate.ps1

# 启动后端
python app.py
```

### 2. 启动前端

打开终端 2：
```bash
cd D:\python_work\创意\a_web\frontend
npm run dev
```

### 3. 访问应用

浏览器打开：http://localhost:5173

---

## 🔧 常见问题解决

### 问题 1：端口被占用

**错误信息**：`Address already in use` 或 `Port 5000 is in use`

**解决方案**：

修改后端端口：
```python
# 编辑 backend/app.py 文件
# 找到这一行：
app.run(debug=True, port=5000)

# 改为其他端口，比如 5001：
app.run(debug=True, port=5001)
```

同时修改前端代理配置：
```javascript
// 编辑 frontend/vite.config.js 文件
// 找到这一部分：
proxy: {
  '/api': {
    target: 'http://127.0.0.1:5000',
    // 改为：
    target: 'http://127.0.0.1:5001',
    changeOrigin: true,
  },
},
```

### 问题 2：pip install 失败

**错误信息**：`pip is not recognized` 或安装超时

**解决方案**：

```bash
# 1. 确保 pip 是最新的
python -m pip install --upgrade pip

# 2. 使用国内镜像源（推荐）
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple/

# 3. 或者使用阿里云镜像
pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/
```

### 问题 3：npm install 失败

**错误信息**：`npm ERR!` 或网络超时

**解决方案**：

```bash
# 1. 清除缓存
npm cache clean --force

# 2. 使用淘宝镜像（推荐）
npm config set registry https://registry.npmmirror.com

# 3. 重新安装
npm install
```

### 问题 4：数据库错误

**错误信息**：`sqlalchemy.exc.OperationalError` 或 `no such table`

**解决方案**：

```bash
cd D:\python_work\创意\a_web\backend

# 删除旧数据库（如果存在）
# Windows:
del instance\cloudnest.db

# macOS/Linux:
rm instance/cloudnest.db

# 重新初始化
flask db init
flask db migrate -m "重新初始化"
flask db upgrade
```

### 问题 5：前端无法连接后端

**症状**：页面显示但无法登录/注册，控制台有网络错误

**解决方案**：

1. 确认后端正在运行（终端 1 应该显示 `Running on http://127.0.0.1:5000`）
2. 检查浏览器控制台（F12）的错误信息
3. 确认 `frontend/vite.config.js` 中的代理配置正确

### 问题 6：Python 虚拟环境激活失败（Windows PowerShell）

**错误信息**：`cannot be loaded because running scripts is disabled`

**解决方案**：

```bash
# 以管理员身份运行 PowerShell，然后执行：
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# 然后重新激活虚拟环境
.venv\Scripts\Activate.ps1
```

---

## 📚 项目功能说明

### 用户系统
- 注册新账号
- 登录/登出
- 个人资料管理

### 云存储
- 文件上传/下载
- 文件夹管理
- 文件分享

### 笔记管理
- 创建/编辑/删除笔记
- 笔记本分类
- 标签管理
- Markdown 支持

### 任务看板
- 创建任务
- 任务状态管理（待办/进行中/完成）
- 任务列自定义

---

## 🛠️ 开发者指南

### 项目结构

```
a_web/
├── backend/                    # Flask 后端
│   ├── api/                   # API 路由定义
│   │   └── v1/               # API v1 版本
│   ├── services/              # 业务逻辑层
│   ├── models.py              # 数据库模型
│   ├── config.py              # 配置文件
│   ├── extensions.py          # Flask 扩展初始化
│   ├── app.py                 # 应用入口
│   ├── requirements.txt       # Python 依赖
│   └── .env                   # 环境变量（不提交到 Git）
│
├── frontend/                   # Vue 3 前端
│   ├── src/
│   │   ├── api/              # API 调用封装
│   │   ├── assets/           # 静态资源
│   │   ├── components/       # 可复用组件
│   │   ├── router/           # 路由配置
│   │   ├── stores/           # Pinia 状态管理
│   │   ├── utils/            # 工具函数
│   │   └── views/            # 页面视图
│   ├── public/               # 公共静态文件
│   ├── package.json          # Node 依赖
│   └── vite.config.js        # Vite 配置
│
├── 使用指南.md                  # 本文件
└── CloudNest_项目规划.md        # 项目规划文档
```

### 添加新 API 端点

1. 在 `backend/api/v1/` 目录下创建新的路由文件
2. 在 `backend/services/` 目录下添加业务逻辑
3. 在 `backend/models.py` 中添加数据库模型（如需要）
4. 运行数据库迁移

### 添加新页面

1. 在 `frontend/src/views/` 目录下创建新页面组件
2. 在 `frontend/src/router/index.js` 中添加路由
3. 在 `frontend/src/api/` 中添加 API 调用（如需要）

---

## 📞 获取帮助

如果遇到本指南未覆盖的问题：

1. 查看终端/控制台的错误信息
2. 检查浏览器开发者工具（F12）的控制台
3. 查看 `backend/` 目录下的日志文件（如果有）
4. 搜索错误信息 + "Flask" 或 "Vue"

---

## 🎉 开始使用

现在你已经了解了如何设置和运行项目，可以开始探索 CloudNest 的功能了！

**快速链接**：
- 🌐 应用地址：http://localhost:5173
- 📧 API 文档：查看 `backend/api/` 目录下的代码注释
- 📋 项目规划：查看 `CloudNest_项目规划.md`

祝你使用愉快！ 🚀
